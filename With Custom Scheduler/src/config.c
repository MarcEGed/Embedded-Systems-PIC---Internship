/*
  config.c - Implementation file for config.h
  config.h contains all needed imports for PIC + a tick speed constant
  This file exists to satisfy the compiler's expectation for a .c file
  It is left intentionally empty
*/
#include "config.h"
